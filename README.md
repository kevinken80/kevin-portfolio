Kevin Kennedy - Portfolio (React + Vite + Tailwind + TypeScript)

Quick start:

1. Install dependencies:
   npm install
2. Install Tailwind tooling (if needed):
   npm install -D tailwindcss postcss autoprefixer
   npx tailwindcss init -p
3. Start dev server:
   npm run dev
4. Redeploy fix — 2025-11-04

Notes:

* Social links are placeholders; replace '#' with your URLs.
* Images use Unsplash collections as AI-style placeholders.
* Contact form is static. I can add EmailJS or Netlify Forms if you want.
